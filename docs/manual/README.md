# Manual Lane Deliverables

Documents produced by the agent for the "Manual lane" in
`.github/copilot-instructions.md` -- content a human applies in the Power BI
Service (Prep for AI, RLS, synonyms), since the MCP has no write operation for
these. Organize as `<model>/<deliverable>.md`, e.g.
`sales-test-model/synonyms.md`, `sales-test-model/security-validation.md`.
